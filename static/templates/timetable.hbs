<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <title>{{name}} Detail Ansicht</title>
    <link rel="stylesheet" href="/static/style.css">
</head>

<body>
<h1>{{name}}</h1>
Das Ventil {{name}} ist gerade {{valve_status}} und wird durch {{automation_status}} gesteurt.
<div class="wrapper">
{{#each schedule}}
        <div class= "column">
            <div class= "heading">{{@key}}</div>
            {{#each this}}
            <div class="value"> Von {{begin}} bis {{end}}</div>
            {{/each}}
        </div>
{{/each}}
</table>
</body>